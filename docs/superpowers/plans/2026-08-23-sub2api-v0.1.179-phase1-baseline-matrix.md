# Sub2API v0.1.179 Phase 1 Baseline And Matrix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 固定 Sub2API `v0.1.179` 官方事实基线，建立可重复运行的差异清单和无遗漏 Runtime 功能矩阵，为后续五个同步阶段提供可审计输入。

**Architecture:** 阶段一不修改生产 Runtime、数据库或前端。使用一个仅依赖 Python 标准库的开发工具从 GitHub Tag/Release/Compare API 和官方源码归档生成 commit/file 清单，再由人工审阅的功能矩阵为每个官方 Runtime 变化指定 `direct_sync`、`adapter_port`、`xcode_equivalent`、`productcore_mapping` 或 `not_runtime` 归宿；验证器阻止未分类条目进入阶段二。

**Tech Stack:** Python 3 标准库、GitHub REST API、SHA-256、CSV/JSON/Markdown、Git worktree、Go 1.26.6。

---

## 文件结构

- Create: `tools/sub2api_upstream_inventory.py` — 下载并校验官方元数据/源码，生成 commit 和 file inventory，验证矩阵覆盖率。
- Create: `tools/test_sub2api_upstream_inventory.py` — 对路径分类、迁移编号、哈希差异、Tag 解析和矩阵覆盖做纯本地单元测试。
- Create: `docs/upstream/sub2api-v0.1.179/README.md` — 记录生成方法、官方身份和审阅规则。
- Create: `docs/upstream/sub2api-v0.1.179/metadata.json` — 固定 Tag、commit、release、archive SHA-256 和生成时间。
- Create: `docs/upstream/sub2api-v0.1.179/commits.csv` — `v0.1.169...v0.1.179` 全部 594 个提交及自动类别。
- Create: `docs/upstream/sub2api-v0.1.179/files.csv` — 官方/当前文件存在性、SHA-256 差异和所有权类别。
- Create: `docs/upstream/sub2api-v0.1.179/runtime-feature-matrix.md` — 人工审阅后的完整 Runtime 功能归宿和后续阶段。
- Create: `docs/upstream/sub2api-v0.1.179/database-impact.md` — 官方 migration `217-228` 与 XCode 数据模型的逐项映射。
- Create: `docs/upstream/sub2api-v0.1.179/xcode-adapter-overrides.md` — 后续同步必须保留的 XCode Adapter/Port 覆盖点。

## 阶段门槛

本计划完成前不得修改 `backend/internal/`、`backend/ent/`、`backend/migrations/`、`frontend/`、依赖或部署配置。阶段二只能在以下条件同时满足时开始：

- `metadata.json` 的 commit 为 `75f88be5f75c27771836b586f7de1503afa0e3bc`。
- `commits.csv` 恰好包含 594 个数据行且无空类别。
- `files.csv` 覆盖官方源码和当前 XCode 后端的文件并带哈希状态。
- Runtime 功能矩阵没有 `unclassified`、空归宿或空后续阶段。
- migration `217-228` 全部有明确映射，且没有直接执行官方 SQL 的计划。

---

### Task 1: 创建隔离工作区并验证基线

**Files:**
- Read: `docs/superpowers/specs/2026-08-23-sub2api-v0.1.179-full-runtime-sync-design.md`
- Read: `docs/ARCHITECTURE.md`
- No tracked file changes.

- [ ] **Step 1: 验证主工作区和基线提交**

Run:

```powershell
git -C C:\Users\xiaohuihui\Desktop\XCODE status --short --branch
git -C C:\Users\xiaohuihui\Desktop\XCODE rev-parse main
git -C C:\Users\xiaohuihui\Desktop\XCODE rev-parse origin/main
```

Expected: `main` 包含设计提交 `28f1c53`；现有 `docs/memory/当前状态.md`、`frontend/pnpm-lock.yaml` 和 `frontend/pnpm-workspace.yaml` 变更保持原样，不进入阶段分支提交。

- [ ] **Step 2: 创建阶段一 worktree**

Run:

```powershell
$path = 'C:\Users\xiaohuihui\.config\superpowers\worktrees\XCODE\codex-sub2api-v01179-phase1'
git -C C:\Users\xiaohuihui\Desktop\XCODE worktree add $path -b codex/sub2api-v0.1.179-phase1 main
git -C $path status --short --branch
```

Expected: 新 worktree 位于指定路径、分支为 `codex/sub2api-v0.1.179-phase1`、工作树干净。

- [ ] **Step 3: 运行后端基线测试**

Run:

```powershell
go test -count=1 -tags=unit ./internal/runtime/sub2api ./internal/handler ./internal/service
go test -count=1 ./pkg/runtimebridge/v1
go build ./cmd/server
```

Working directory: `C:\Users\xiaohuihui\.config\superpowers\worktrees\XCODE\codex-sub2api-v01179-phase1\backend`

Expected: 所有命令退出码为 0。若失败，记录现有失败并停止阶段一，不把失败归因于尚未发生的同步。

---

### Task 2: 以 TDD 建立清单工具的纯函数核心

**Files:**
- Create: `tools/sub2api_upstream_inventory.py`
- Create: `tools/test_sub2api_upstream_inventory.py`

- [ ] **Step 1: 编写路径、迁移和矩阵覆盖失败测试**

Create `tools/test_sub2api_upstream_inventory.py` with these cases:

```python
import csv
import tempfile
import unittest
from pathlib import Path

from tools.sub2api_upstream_inventory import (
    classify_path,
    compare_trees,
    migration_number,
    resolve_tag_commit,
    validate_matrix,
)


class InventoryTests(unittest.TestCase):
    def test_classify_path_separates_runtime_product_and_database(self):
        self.assertEqual(classify_path("backend/internal/pkg/apicompat/a.go"), "runtime_protocol")
        self.assertEqual(classify_path("backend/internal/service/openai_gateway.go"), "runtime_provider")
        self.assertEqual(classify_path("backend/internal/service/subscription.go"), "productcore")
        self.assertEqual(classify_path("backend/migrations/228_channel_pricing_multipliers.sql"), "database")
        self.assertEqual(classify_path("frontend/src/views/admin/GroupsView.vue"), "frontend_product")

    def test_migration_number_handles_numeric_prefix_and_non_migration(self):
        self.assertEqual(migration_number("backend/migrations/228_channel.sql"), 228)
        self.assertEqual(migration_number("backend/migrations/225a_index.sql"), 225)
        self.assertIsNone(migration_number("backend/internal/service/openai.go"))

    def test_resolve_tag_commit_accepts_lightweight_and_annotated_tags(self):
        self.assertEqual(resolve_tag_commit({"object": {"type": "commit", "sha": "abc"}}), "abc")
        self.assertEqual(
            resolve_tag_commit({"object": {"type": "tag", "sha": "tagsha"}}, {"object": {"sha": "def"}}),
            "def",
        )

    def test_compare_trees_reports_official_only_current_only_and_different(self):
        with tempfile.TemporaryDirectory() as temp:
            root = Path(temp)
            official = root / "official"
            current = root / "current"
            official.mkdir()
            current.mkdir()
            (official / "same.go").write_text("same", encoding="utf-8")
            (current / "same.go").write_text("same", encoding="utf-8")
            (official / "changed.go").write_text("official", encoding="utf-8")
            (current / "changed.go").write_text("current", encoding="utf-8")
            (official / "new.go").write_text("new", encoding="utf-8")
            (current / "local.go").write_text("local", encoding="utf-8")
            states = {row["path"]: row["state"] for row in compare_trees(official, current)}
            self.assertEqual(states["same.go"], "same")
            self.assertEqual(states["changed.go"], "different")
            self.assertEqual(states["new.go"], "official_only")
            self.assertEqual(states["local.go"], "current_only")

    def test_validate_matrix_rejects_empty_disposition_and_uncovered_commit(self):
        commits = [
            {"sha": "a", "category": "runtime_protocol"},
            {"sha": "b", "category": "runtime_provider"},
        ]
        features = [{"id": "F001", "commits": "a", "disposition": "direct_sync", "phase": "2"}]
        with self.assertRaisesRegex(ValueError, "uncovered commits: b"):
            validate_matrix(commits, features)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行测试并确认红灯**

Run:

```powershell
python -m unittest tools/test_sub2api_upstream_inventory.py -v
```

Expected: FAIL，原因是 `sub2api_upstream_inventory` 尚不存在，而不是测试语法错误。

- [ ] **Step 3: 实现最小纯函数核心**

Create `tools/sub2api_upstream_inventory.py` with these public contracts:

```python
#!/usr/bin/env python3
import argparse
import csv
import hashlib
import json
import re
import urllib.request
import zipfile
from pathlib import Path

DISPOSITIONS = {
    "direct_sync",
    "adapter_port",
    "xcode_equivalent",
    "productcore_mapping",
    "not_runtime",
}

RUNTIME_PROTOCOL_PREFIXES = (
    "backend/internal/pkg/apicompat/",
    "backend/internal/pkg/openai/",
    "backend/internal/pkg/xai/",
)
RUNTIME_PROVIDER_PREFIXES = (
    "backend/internal/runtime/",
    "backend/internal/handler/openai_",
    "backend/internal/handler/grok_",
    "backend/internal/handler/gemini_",
    "backend/internal/service/openai_",
    "backend/internal/service/grok_",
    "backend/internal/service/gemini_",
    "backend/internal/service/antigravity_",
)
PRODUCT_MARKERS = (
    "/subscription",
    "/api_key",
    "/payment",
    "/pricing",
    "/group",
    "/channel",
    "/redeem",
)


def classify_path(path: str) -> str:
    normalized = path.replace("\\", "/")
    if normalized.startswith("backend/migrations/") or normalized.startswith("backend/ent/schema/"):
        return "database"
    if normalized.startswith("frontend/"):
        return "frontend_product"
    if normalized.startswith(RUNTIME_PROTOCOL_PREFIXES):
        return "runtime_protocol"
    if normalized.startswith(RUNTIME_PROVIDER_PREFIXES):
        return "runtime_provider"
    if normalized.startswith("backend/internal/") and any(marker in normalized for marker in PRODUCT_MARKERS):
        return "productcore"
    if normalized.startswith(("deploy/", ".github/", "Dockerfile", "Makefile")):
        return "infrastructure"
    if normalized.endswith("_test.go") or "/testdata/" in normalized:
        return "test"
    if normalized.startswith(("docs/", "README")):
        return "documentation"
    return "needs_review"


def migration_number(path: str):
    match = re.search(r"(?:^|/)migrations/(\d+)[a-zA-Z_].*\.sql$", path.replace("\\", "/"))
    return int(match.group(1)) if match else None


def sha256_file(path: Path) -> str:
    digest = hashlib.sha256()
    with path.open("rb") as handle:
        for chunk in iter(lambda: handle.read(1024 * 1024), b""):
            digest.update(chunk)
    return digest.hexdigest()


def compare_trees(official: Path, current: Path):
    official_files = {p.relative_to(official).as_posix(): p for p in official.rglob("*") if p.is_file()}
    current_files = {p.relative_to(current).as_posix(): p for p in current.rglob("*") if p.is_file()}
    rows = []
    for path in sorted(set(official_files) | set(current_files)):
        left = official_files.get(path)
        right = current_files.get(path)
        if left is None:
            state = "current_only"
        elif right is None:
            state = "official_only"
        else:
            state = "same" if sha256_file(left) == sha256_file(right) else "different"
        rows.append({"path": path, "state": state, "category": classify_path(path)})
    return rows


def resolve_tag_commit(ref_payload: dict, tag_payload: dict | None = None) -> str:
    obj = ref_payload["object"]
    if obj["type"] == "commit":
        return obj["sha"]
    if obj["type"] == "tag" and tag_payload:
        return tag_payload["object"]["sha"]
    raise ValueError("annotated tag payload is required")


def validate_matrix(commits: list[dict], features: list[dict]) -> None:
    invalid = [row.get("id", "<missing>") for row in features if row.get("disposition") not in DISPOSITIONS or not row.get("phase")]
    if invalid:
        raise ValueError("invalid feature rows: " + ", ".join(invalid))
    covered = {sha.strip() for row in features for sha in row.get("commits", "").split() if sha.strip()}
    runtime_commits = [row for row in commits if row.get("category", "").startswith("runtime_")]
    missing = sorted(row["sha"] for row in runtime_commits if row["sha"] not in covered)
    if missing:
        raise ValueError("uncovered commits: " + ", ".join(missing))
```

The same file must expose `snapshot`, `generate`, and `validate` CLI subcommands in Task 3; do not add network behavior in this step.

- [ ] **Step 4: 运行单元测试并确认绿灯**

Run:

```powershell
python -m unittest tools/test_sub2api_upstream_inventory.py -v
```

Expected: 5 tests PASS。

- [ ] **Step 5: 提交纯函数和测试**

```powershell
git add tools/sub2api_upstream_inventory.py tools/test_sub2api_upstream_inventory.py
git commit -m "feat(runtime): 增加官方版本差异清单工具"
```

---

### Task 3: 实现官方快照和清单生成命令

**Files:**
- Modify: `tools/sub2api_upstream_inventory.py`
- Modify: `tools/test_sub2api_upstream_inventory.py`

- [ ] **Step 1: 添加 CLI 解析和快照失败测试**

Add tests that use a local fake GitHub payload directory and assert:

```python
def test_snapshot_rejects_wrong_target_commit(self):
    with self.assertRaisesRegex(ValueError, "target commit mismatch"):
        verify_target_commit("wrong", "75f88be5f75c27771836b586f7de1503afa0e3bc")

def test_write_csv_uses_stable_column_order(self):
    with tempfile.TemporaryDirectory() as temp:
        output = Path(temp) / "rows.csv"
        write_csv(output, [{"sha": "a", "category": "runtime_protocol"}], ["sha", "category"])
        self.assertEqual(output.read_text(encoding="utf-8").splitlines()[0], "sha,category")
```

- [ ] **Step 2: 运行新增测试并确认红灯**

Run: `python -m unittest tools/test_sub2api_upstream_inventory.py -v`

Expected: FAIL because `verify_target_commit` and `write_csv` are undefined.

- [ ] **Step 3: 实现三个 CLI 子命令**

Implement these exact interfaces:

```text
snapshot --repo Wei-Shaw/sub2api --base v0.1.169 --target v0.1.179 --expected-commit 75f88be5f75c27771836b586f7de1503afa0e3bc --cache-dir C:\Users\xiaohuihui\AppData\Local\Temp\xcode-sub2api-v0.1.179-inventory
generate --snapshot-dir C:\Users\xiaohuihui\AppData\Local\Temp\xcode-sub2api-v0.1.179-inventory --current-root C:\Users\xiaohuihui\.config\superpowers\worktrees\XCODE\codex-sub2api-v01179-phase1 --output-dir docs/upstream/sub2api-v0.1.179
validate --inventory-dir docs/upstream/sub2api-v0.1.179
```

Required behavior:

- `snapshot` calls GitHub release, tag ref, annotated tag when needed, and compare endpoints with `User-Agent: XCode-Upstream-Inventory`; optional token comes only from `GITHUB_TOKEN` and is never printed or persisted.
- `snapshot` downloads `https://github.com/Wei-Shaw/sub2api/archive/refs/tags/v0.1.179.zip`, computes SHA-256, extracts it under the ignored cache directory, and rejects any resolved commit other than the expected full SHA.
- `snapshot` writes raw API payloads only under the ignored cache directory.
- `generate` writes deterministic UTF-8/LF JSON and CSV files. `metadata.json` contains `repo`, `base_tag`, `target_tag`, `target_commit`, `release_published_at`, `archive_sha256`, `official_version_file`, `commit_count`, and `generated_at`.
- `commits.csv` columns are `sha,date,subject,category,files_known`; every row receives a category, including `needs_review`.
- `files.csv` columns are `path,state,category,official_sha256,current_sha256,migration_number`; exclude `.git`, caches, binaries and generated frontend `dist`.
- `validate` exits non-zero for wrong commit, commit count other than 594, `needs_review`, incomplete feature matrix, missing migration mapping, or official SQL marked for direct execution.

Implement helper functions as deterministic units and keep CLI orchestration under `main()`.

- [ ] **Step 4: 运行工具单元测试**

Run: `python -m unittest tools/test_sub2api_upstream_inventory.py -v`

Expected: all tests PASS without network access.

- [ ] **Step 5: 运行真实官方快照**

Run from worktree root:

```powershell
$cache = Join-Path $env:TEMP 'xcode-sub2api-v0.1.179-inventory'
python tools/sub2api_upstream_inventory.py snapshot `
  --repo Wei-Shaw/sub2api `
  --base v0.1.169 `
  --target v0.1.179 `
  --expected-commit 75f88be5f75c27771836b586f7de1503afa0e3bc `
  --cache-dir $cache
python tools/sub2api_upstream_inventory.py generate `
  --snapshot-dir $cache `
  --current-root . `
  --output-dir docs/upstream/sub2api-v0.1.179
```

Expected: metadata target SHA matches exactly; commit count is 594; official source `VERSION` is recorded as `0.1.178`; generated CSV files use stable ordering.

- [ ] **Step 6: 提交 CLI 完成版和机器清单**

```powershell
git add tools/sub2api_upstream_inventory.py tools/test_sub2api_upstream_inventory.py docs/upstream/sub2api-v0.1.179/metadata.json docs/upstream/sub2api-v0.1.179/commits.csv docs/upstream/sub2api-v0.1.179/files.csv
git commit -m "chore(runtime): 固定 v0.1.179 官方差异基线"
```

---

### Task 4: 审阅所有自动分类并消除 needs_review

**Files:**
- Modify: `tools/sub2api_upstream_inventory.py`
- Modify: `tools/test_sub2api_upstream_inventory.py`
- Regenerate: `docs/upstream/sub2api-v0.1.179/commits.csv`
- Regenerate: `docs/upstream/sub2api-v0.1.179/files.csv`

- [ ] **Step 1: 输出未分类集合**

Run:

```powershell
Import-Csv docs/upstream/sub2api-v0.1.179/commits.csv | Where-Object category -eq 'needs_review' | Format-Table sha,subject
Import-Csv docs/upstream/sub2api-v0.1.179/files.csv | Where-Object category -eq 'needs_review' | Group-Object {($_.path -split '/')[0..1] -join '/'} | Sort-Object Count -Descending
```

Expected: 初次可能有未分类项；保留完整输出作为本任务的分类输入。

- [ ] **Step 2: 为每类未知路径先补失败测试**

For every unknown prefix, add a concrete assertion to `test_classify_path_separates_runtime_product_and_database`. Examples must use actual paths from Step 1, not invented placeholders.

- [ ] **Step 3: 运行分类测试并确认红灯**

Run: `python -m unittest tools/test_sub2api_upstream_inventory.py -v`

Expected: new assertions FAIL with `needs_review`.

- [ ] **Step 4: 扩展分类规则并重新生成清单**

Update only explicit prefix/marker tables. Do not add a catch-all that silently maps unknown code to Runtime.

Run:

```powershell
$cache = Join-Path $env:TEMP 'xcode-sub2api-v0.1.179-inventory'
python -m unittest tools/test_sub2api_upstream_inventory.py -v
python tools/sub2api_upstream_inventory.py generate --snapshot-dir $cache --current-root . --output-dir docs/upstream/sub2api-v0.1.179
```

Expected: tests PASS; `needs_review` count is zero for commits and files.

- [ ] **Step 5: 提交分类规则和再生成结果**

```powershell
git add tools/sub2api_upstream_inventory.py tools/test_sub2api_upstream_inventory.py docs/upstream/sub2api-v0.1.179/commits.csv docs/upstream/sub2api-v0.1.179/files.csv
git commit -m "chore(runtime): 完成 v0.1.179 差异分类"
```

---

### Task 5: 建立 Runtime 功能矩阵

**Files:**
- Create: `docs/upstream/sub2api-v0.1.179/runtime-feature-matrix.md`
- Create: `docs/upstream/sub2api-v0.1.179/xcode-adapter-overrides.md`

- [ ] **Step 1: 创建矩阵表头和 Release 功能行**

The matrix table must use these columns:

```markdown
| ID | 官方功能 | 官方提交 | 官方路径 | XCode 当前路径 | 归宿 | 阶段 | 数据库影响 | 验收测试 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
```

Create at least these release-level rows with concrete commit SHAs from `commits.csv`:

- 国产 Provider adaptive Chat/Messages/Responses 与三套 Base URL。
- Kimi/GLM/DeepSeek Header 覆写、额度/余额和账号测试。
- Composite Codex 端点与 Composite 国产 Provider。
- Responses input token preflight。
- Chat 非流式读取失败切换和 message-only capacity 恢复。
- Responses WebSocket later-turn 429 切换、client tools replay 和 item ID 修复。
- Responses→Chat encrypted-only reasoning 回注。
- Grok inline image/view_image、tool search output/discovery、4.6 xhigh。
- 代理探测目标配置。
- 服务层级/上下文倍率、Fast usage facts 和长上下文破坏性变更映射。

Every row must use one of the five approved dispositions and one target phase from 2 through 6.

- [ ] **Step 2: 用 commit inventory 补齐 Release Notes 未展开的 Runtime 提交**

For each commit category `runtime_protocol` or `runtime_provider`, add its SHA to an existing feature row or create a focused row. Merge commits may map to the same row as their contained commits, but no SHA may remain uncovered.

- [ ] **Step 3: 记录 XCode Adapter/Port 覆盖点**

Create `xcode-adapter-overrides.md` with exact ownership and paths for:

- `backend/pkg/runtimebridge/v1/`
- `backend/internal/applicationgateway/`
- `backend/internal/runtime/sub2api/`
- `backend/internal/handler/runtime_ingress*.go`
- `backend/internal/handler/sub2api_*`
- ProductCore platform/model/usage/billing paths discovered in `files.csv`

For every override record: purpose, official counterpart, keep/replace rule, contract test and owning phase.

- [ ] **Step 4: 添加矩阵解析测试并运行红灯**

Extend the tool to parse fenced-free Markdown table rows and call `validate_matrix`. Add a test containing an uncovered commit and assert the validator exits non-zero.

Run: `python -m unittest tools/test_sub2api_upstream_inventory.py -v`

Expected: FAIL before parser implementation, then PASS after minimal parser is added.

- [ ] **Step 5: 运行真实矩阵覆盖验证**

Run:

```powershell
python tools/sub2api_upstream_inventory.py validate --inventory-dir docs/upstream/sub2api-v0.1.179
```

Expected: zero uncovered Runtime commits, zero invalid dispositions, zero empty phases.

- [ ] **Step 6: 提交功能矩阵和覆盖清单**

```powershell
git add tools/sub2api_upstream_inventory.py tools/test_sub2api_upstream_inventory.py docs/upstream/sub2api-v0.1.179/runtime-feature-matrix.md docs/upstream/sub2api-v0.1.179/xcode-adapter-overrides.md
git commit -m "docs(runtime): 建立 v0.1.179 功能归宿矩阵"
```

---

### Task 6: 完成数据库影响映射

**Files:**
- Create: `docs/upstream/sub2api-v0.1.179/database-impact.md`
- Modify: `docs/upstream/sub2api-v0.1.179/runtime-feature-matrix.md`

- [ ] **Step 1: 列出官方新增迁移和 XCode 冲突**

Run:

```powershell
Import-Csv docs/upstream/sub2api-v0.1.179/files.csv |
  Where-Object { $_.path -match '^backend/migrations/(217|218|219|220|221|222|223|224|225|226|227|228)' } |
  Sort-Object path |
  Format-Table path,state,migration_number
Get-ChildItem backend/migrations -File | Sort-Object Name | Select-Object -Last 45 -ExpandProperty Name
```

Expected: identify every official `217-228` migration and confirm XCode already owns custom `192-200` and `9000` ranges.

- [ ] **Step 2: 写入逐项映射表**

Use this table shape:

```markdown
| 官方迁移 | 官方目的 | Runtime 是否必需 | XCode 等价状态 | 处理方式 | 目标迁移 | 数据风险 | 验证 |
| --- | --- | --- | --- | --- | --- | --- | --- |
```

Rules:

- `217-224` product pricing/rollup/quota changes map to ProductCore or `not_runtime`; never copy SQL.
- `225` Codex fingerprint seed is reviewed for `accounts.extra` reuse; only use an `8000-8999` migration if indexed/queryable persistent state is proven necessary.
- both official `226` migrations are reviewed separately: usage expression index maps to XCode usage query needs; channel-monitor quota mode maps to Runtime account observation without restoring Channel ownership.
- `227` Composite CN Providers maps to platform Runtime capability/configuration.
- `228` Channel pricing multipliers maps to ProductCore pricing and does not enter Runtime billing defaults.

- [ ] **Step 3: 补充迁移验证器测试**

Add a fixture where one of `217-228` is missing or has handling `direct_sql`, and assert `validate` fails. Implement validation that requires all discovered official migration files to appear in `database-impact.md` and rejects `direct_sql`.

- [ ] **Step 4: 运行数据库映射验证**

Run:

```powershell
python -m unittest tools/test_sub2api_upstream_inventory.py -v
python tools/sub2api_upstream_inventory.py validate --inventory-dir docs/upstream/sub2api-v0.1.179
```

Expected: all tests PASS; no official migration is scheduled for direct execution.

- [ ] **Step 5: 提交数据库影响文档**

```powershell
git add tools/sub2api_upstream_inventory.py tools/test_sub2api_upstream_inventory.py docs/upstream/sub2api-v0.1.179/database-impact.md docs/upstream/sub2api-v0.1.179/runtime-feature-matrix.md
git commit -m "docs(runtime): 映射 v0.1.179 数据库影响"
```

---

### Task 7: 收口阶段一并生成后续计划输入

**Files:**
- Create: `docs/upstream/sub2api-v0.1.179/README.md`
- Modify: `docs/upstream/sub2api-v0.1.179/metadata.json`
- Read: all Phase 1 artifacts.

- [ ] **Step 1: 写入可重复生成说明**

`README.md` must include:

- immutable official identity and the `VERSION=0.1.178` discrepancy;
- snapshot/generate/validate commands;
- generated versus manually reviewed files;
- disposition definitions;
- phase gate and prohibition on direct upstream merge/migration execution;
- how a future official Tag replaces base/target arguments.

- [ ] **Step 2: 运行完整阶段验证**

Run:

```powershell
python -m unittest tools/test_sub2api_upstream_inventory.py -v
python tools/sub2api_upstream_inventory.py validate --inventory-dir docs/upstream/sub2api-v0.1.179
go test -count=1 -tags=unit ./internal/runtime/sub2api ./internal/handler ./internal/service
go test -count=1 ./pkg/runtimebridge/v1
go build ./cmd/server
git diff --check
git status --short
```

Working directory for Go commands: `C:\Users\xiaohuihui\.config\superpowers\worktrees\XCODE\codex-sub2api-v01179-phase1\backend`.

Expected: all commands exit 0; tracked changes consist only of Phase 1 tools/docs; no Runtime, schema, migration, frontend, dependency or deployment changes.

- [ ] **Step 3: 检查完成定义**

Run:

```powershell
(Import-Csv docs/upstream/sub2api-v0.1.179/commits.csv).Count
(Import-Csv docs/upstream/sub2api-v0.1.179/commits.csv | Where-Object { -not $_.category -or $_.category -eq 'needs_review' }).Count
(Import-Csv docs/upstream/sub2api-v0.1.179/files.csv | Where-Object category -eq 'needs_review').Count
git diff --name-only main...HEAD -- backend/internal backend/ent backend/migrations frontend backend/go.mod backend/go.sum deploy .github
```

Expected outputs: `594`, `0`, `0`, and no paths for the final command.

- [ ] **Step 4: 提交阶段一说明**

```powershell
git add docs/upstream/sub2api-v0.1.179/README.md docs/upstream/sub2api-v0.1.179/metadata.json
git commit -m "docs(runtime): 完成 v0.1.179 同步基线"
```

- [ ] **Step 5: 按矩阵分别编写阶段二至阶段六计划**

Create one plan per phase under `docs/superpowers/plans/`, using only exact paths and commits from the validated matrix:

```text
2026-08-23-sub2api-v0.1.179-phase2-protocol-core.md
2026-08-23-sub2api-v0.1.179-phase3-provider-pool.md
2026-08-23-sub2api-v0.1.179-phase4-media-extensions.md
2026-08-23-sub2api-v0.1.179-phase5-runtime-admin.md
2026-08-23-sub2api-v0.1.179-phase6-database-release.md
```

Do not start Phase 2 implementation in the same checkpoint. Present the validated matrix, database map, commit list and the five new plans for review first.
