# 官方重点更新同步边界

## 决策

- `my2.0` 不直接合并官方主分支，按提交和行为挑选安全、稳定性与核心网关修复。
- 不引入 Grok 扩展、Group 利润控制、旧 Channel 路由或旧计费权威。
- 官方改动必须保持 Platform 为业务路由权威、SubscriptionPlan/余额为资产权威、ModelPricingCatalog 为模型基础价格权威。
- 涉及 schema、Ent、迁移或用量持久化契约的官方功能单独评审、演练和发布，不与常规故障修复捆绑。

## 本轮范围

- 同步官方截至 `b5e83156d` 的重点安全、网络、Codex/OpenAI、Responses、Gemini、计费精度和订阅重置修复。
- 上游实际响应模型审计 `db0bff82c` 暂缓，原因是其改动跨越自定义平台身份、用量记录和计费边界。

## 后续同步规则

1. 先更新 `upstream/main` 并按提交分类，不直接 merge。
2. 优先同步安全漏洞、数据正确性、协议兼容和故障切换修复。
3. 对照架构守卫，拒绝重新引入 Group/Channel 或旧计费路径。
4. 先移植官方回归测试，再按本分支架构实现最小修复。
5. 完成后运行后端/前端全量验证；schema 改动必须先在数据库副本演练。
