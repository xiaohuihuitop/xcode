# Runtime Adapter Boundary Verification

- Date: 2026-08-10
- Branch: `my2.0`
- Plan: `docs/superpowers/plans/2026-08-10-runtime-adapter-boundary.md`

## Completed in this pass

- `ApplicationGateway` wraps the product usage sink with a thread-safe `TerminalRecorder` and rejects an executor that returns without a terminal event.
- `Sub2APIRuntimeAdapter` performs the same terminal-state check for the compatibility runtime and preserves the original executor error with `errors.Join` when both are present.
- Responses, Chat Completions, Messages, and their raw compatibility fallbacks clear a stale OpenAI upstream-endpoint marker at the start of an attempt and set the actual endpoint before sending upstream.
- The shared production path for Messages, Chat Completions, and Responses remains `ApplicationGateway -> Sub2APIRuntimeAdapter -> compatibility executor`.

## Verification evidence

- Passed unit packages: `internal/gatewayruntime`, `internal/applicationgateway`, `internal/productcore`, `internal/architecture`, `migrations`, `internal/handler`, and `internal/service`.
- Passed command checks: `go test ./migrations ./cmd/... -count=1 -timeout=20m` and `go test ./cmd/server -run '^$' -count=1`.
- `git diff --check` completed without whitespace errors.

## Remaining boundary work

Gemini, media, CountTokens, account probing, Live, and Responses WebSocket still use compatibility handlers rather than dedicated pure Runtime executors. Full frontend, Docker, server, commit, tag, and push verification are intentionally pending.
