# 全局背景样式

- 状态：已确认
- 日期：2026-07-20
- 结论：保留旧版暖色控制台主题：琥珀主色、暖棕深色背景、石色浅色表面、半透明网格层和顶部主色渐变。
- 根因：此前仅恢复了 `AppLayout.vue`，但 `tailwind.config.js` 与 `style.css` 仍是官方青绿/深蓝主题，深色页面因此继续显示蓝黑背景；订阅摘要卡还缺少语言包键，直接显示内部 key。
- 正确做法：同步官方后必须同时检查 `frontend/tailwind.config.js`、`frontend/src/style.css` 和 `frontend/src/components/layout/AppLayout.vue`；同时为新增 UI 文案补齐所有语言包键，不能只恢复局部容器。
- 验证：`AppLayoutBackground.spec.ts` 断言布局背景层；`customThemeBackground.spec.ts` 断言暖色主题与深色渐变；`subscriptionSummaryLocales.spec.ts` 断言中英文订阅摘要文案；运行类型检查、lint 和前端生产构建。
- 相关文件：
  - `frontend/src/style.css`
  - `frontend/tailwind.config.js`
  - `frontend/src/components/layout/AppLayout.vue`
  - `frontend/src/components/layout/__tests__/AppLayoutBackground.spec.ts`
  - `frontend/src/components/layout/__tests__/customThemeBackground.spec.ts`
  - `frontend/src/i18n/locales/zh/dashboard.ts`
  - `frontend/src/i18n/locales/en/dashboard.ts`
