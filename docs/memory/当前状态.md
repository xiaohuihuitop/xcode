# 当前状态

## 记忆维护规则

- 后续执行优先读取本文件的 `## 当前状态`。
- 长期架构和范围写入 `docs/PROJECT_DEFINITION.md`、`docs/ARCHITECTURE.md`；本文件只保留当前可执行状态。
- 新规则或稳定验证结论写入 `docs/memory/决策/`，不保存完整聊天过程。

## 当前状态

### 日期

- 2026-08-20

### 当前目标

- 将 XCode 新仓库整理成可独立维护、可交接、可按 Tag 生成离线 Docker 包的产品仓库。
- 清空远程旧 Git 提交和旧 Tag，但保留当前代码、许可证、文档和运行数据兼容边界。

### 已确认实施计划

| 阶段 | 状态 | 目标 | 验收点 |
| --- | --- | --- | --- |
| 项目文档 | 已完成 | 写入项目定义、架构、开发、路线和交接文档 | 无占位符、无敏感信息、README 不再宣传旧项目 |
| 历史重建 | 进行中 | orphan 根提交替换远程 `main` | 新历史无旧贡献者提交链，代码树保持一致 |
| 发布线 | 待执行 | 删除旧 Tag，创建干净 `v1.0.0` | Actions 全部通过并生成 `xcode_latest.tar` |

### 进行中

- 正在完成文档基线检查和 Git 历史重建准备。

### 已完成

- `xiaohuihuitop/xcode` 已建立 `main` 分支，当前代码基线来自已验证的 `my2.0`。
- 产品发布契约已确定为 `xcode:latest` 与 `xcode_latest.tar`，不发布 GHCR。
- ProductCore / GatewayRuntime / Sub2API Adapter 边界和 OpenAI 当前范围已写入正式文档。

### 下一步

1. 检查 README、文档和 Git diff。
2. 提交文档基线。
3. 创建 orphan 根提交并强制推送 `main`。
4. 清理旧 Tag，创建并推送 `v1.0.0`。
5. 等待并核验 GitHub Actions 离线包。

### 阻塞与待确认

- 历史重写是不可逆的远程 Git 操作；用户已确认执行。
- GitHub 贡献者统计刷新可能有延迟。
- 上线前仍需处理订单套餐快照、退款幂等和生产 HTTPS 等风险，详见 `docs/ROADMAP.md`。

### 关联决策

- `docs/memory/决策/2026-08-20-XCode独立仓库与v1发布线.md`
- `docs/memory/决策/2026-08-20-XCode独立仓库迁移交接.md`
