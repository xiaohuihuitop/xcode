# 当前状态

## 记忆维护规则

- 后续执行优先读取本文件的 `## 当前状态`。
- 长期架构和范围写入 `docs/PROJECT_DEFINITION.md`、`docs/ARCHITECTURE.md`；本文件只保留当前可执行状态。
- 新规则或稳定验证结论写入 `docs/memory/决策/`，不保存完整聊天过程。

## 当前状态

### 日期

- 2026-08-20

### 当前目标

- 维持 XCode 的独立产品仓库、可交接文档和按 Tag 生成离线 Docker 包的发布线。

### 已确认实施计划

| 阶段 | 状态 | 目标 | 验收点 |
| --- | --- | --- | --- |
| 项目文档 | 已完成 | 写入项目定义、架构、开发、路线和交接文档 | 本地链接检查通过，README 不再宣传旧项目 |
| 历史重建 | 已完成 | orphan 根提交替换远程 `main` | 新根提交与重建前文件树一致，旧远程 Tag 已删除 |
| 发布线 | 已完成 | 创建干净 `v1.0.0` | XCode Release 与 CI 成功，Release 已上传 `xcode_latest.tar` |

### 进行中

- 将旧仓库误执行的 RuntimeBridge/Sub2API Driver 改造回迁到当前 `XCODE` 仓库，并完成当前基线兼容验证。

### 已完成

- `xiaohuihuitop/xcode` 已建立 `main` 分支，当前代码基线来自已验证的 `my2.0`。
- 产品发布契约已确定为 `xcode:latest` 与 `xcode_latest.tar`，不发布 GHCR。
- ProductCore / GatewayRuntime / Sub2API Adapter 边界和 OpenAI 当前范围已写入正式文档。
- 远程 `main` 已重建为单根提交，新的注释 Tag `v1.0.0` 指向该根提交；旧 `v1.0.0`、`v1.0.1` 已删除。
- `v1.0.0` 的 XCode Release、后端单元测试、PostgreSQL 集成测试、前端门禁和 lint 全部成功，已发布 `xcode_latest.tar` 与 SHA-256 文件。
- RuntimeBridge v1 契约、本地端口、Sub2API Driver、OpenAI 端口适配和架构守卫已回迁到当前工作区；完整后端门禁待本轮验证。

### 下一步

1. 完成本轮后端单元测试、构建和差异检查，再提交并推送当前 `main`。
2. 按发布工作流创建新的 XCode 版本 Tag 并核对离线包；真实服务器验收需另行确认凭据和环境。
3. 在上线前按 `docs/ROADMAP.md` 处理订单套餐快照、退款幂等、HTTPS 与数据库恢复演练。

### 阻塞与待确认

- GitHub 贡献者统计刷新可能有延迟。
- 本次 `v1.0.0` 的独立 Security Scan 仍失败：前端失败于 audit exceptions 校验，后端失败于 `govulncheck`；这两个 workflow 不在 XCode Release 依赖链中，未被本次文档或历史重建修改。
- 上线前仍需处理订单套餐快照、退款幂等和生产 HTTPS 等风险，详见 `docs/ROADMAP.md`。

### 关联决策

- `docs/memory/决策/2026-08-20-XCode独立仓库与v1发布线.md`
- `docs/memory/决策/2026-08-20-XCode独立仓库迁移交接.md`
