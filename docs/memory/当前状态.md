# 当前状态

## 记忆维护规则

- 后续执行优先读取本文件的 `## 当前状态`。
- 长期架构和范围写入 `docs/PROJECT_DEFINITION.md`、`docs/ARCHITECTURE.md`；本文件只保留当前可执行状态。
- 新规则或稳定验证结论写入 `docs/memory/决策/`，不保存完整聊天过程。

## 当前状态

### 日期

- 2026-09-05

### 当前目标

- 维持现有生产环境可用，继续使用 Sub2API 作为唯一 Runtime；官方 `v0.2.0` 的 P0 F001 与 F006 已完成本地同步、Adapter 移植、完整验证和本地提交，尚未推送、打 Tag、创建 Release、编译离线包或部署。GLM 真实上游仍因现有凭据返回 401，不能宣称真实验收通过；F002-F005 未开始。

### 已确认实施计划

| 阶段 | 状态 | 目标 | 验收点 |
| --- | --- | --- | --- |
| 项目文档 | 已完成 | 写入项目定义、架构、开发、路线和交接文档 | 本地链接检查通过，README 不再宣传旧项目 |
| 历史重建 | 已完成 | orphan 根提交替换远程 `main` | 新根提交与重建前文件树一致，旧远程 Tag 已删除 |
| RuntimeBridge 回迁 | 已完成 | 将纯契约、本地端口和 Sub2API Driver 回迁到 XCODE | 后端单元测试、架构守卫和服务构建通过 |
| 发布线 | 已完成 | 发布 `v1.0.1` | XCode Release 与 Tag CI 成功，Release 已上传离线包 |
| 香港迁移验收 | 已完成 | 迁移既有数据并部署 `v1.0.1` 离线包 | 三个容器健康，数据对账和 Chat/Responses 验收通过 |
| 香港生产升级 | 已完成 | 将香港应用容器从 `v1.0.1` 升级到 `v1.0.3` | 新镜像加载校验通过，应用/数据库/Redis 健康，域名 HTTPS 与未授权入口验收通过 |
| Sub2API 路线发布 | 已完成 | 发布并部署继续使用 Sub2API 的 `v1.0.4` | Release 全门禁通过，香港镜像 revision、数据对账、GPT 请求、套餐扣费和管理面验收通过 |
| 官方 v0.1.179 同步基线 | 进行中 | 固定官方版本清单、Runtime 分层和可重复同步工具 | P0 GPT/Codex 已完成代码、回归和隔离验收；Provider、扩展端点、发布与生产升级尚未执行 |
| 官方 v0.2.0 F001 同步 | 已完成（本地） | 同步 OpenAI/Codex bootstrap、WS 关闭语义和活动协议桥 | 完整 backend unit、协议/架构集中测试、server 构建、46 个同步工具测试与两套 validator 通过；真实 Provider NOT RUN |
| 官方 v0.2.0 F006 同步 | 已完成（本地） | 同步共享重试、额度冷却、安全审计与原生 reasoning `max` | F006-A/B/C 红绿回归、Repository 集成测试、完整 backend unit、server 构建、同步工具测试与两套 validator 通过；真实 Provider NOT RUN |
| Sub2API 同步 Skill | 已完成 | 将正式 Tag 差异盘点、本地分组同步和验证编排为个人 Skill | RED/GREEN 压力测试、规格/质量复审和官方 `quick_validate.py` 均通过；远程与生产操作保持独立授权 |
| 香港域名接入 | 已完成 | 通过 Nginx 为 `api.ai-work.vip` 提供 HTTPS 反向代理 | HTTP 301、HTTPS `/health` 200、Chat/Responses 未认证请求到达应用并返回 401 |
| 香港业务验收 | 已完成 | 验证真实网关请求、流式响应、套餐扣费和平台归属 | Chat、Responses、SSE 均 200；usage log、订阅用量和扣费来源一致 |
| 管理面与指定模型验收 | 部分完成 | 验证后台、`gpt-5.6-sol` 和 `glm-5.2` | 后台 API 与 `gpt-5.6-sol` 已通过；GLM 缺少可用上游账号 |
| 模型双价格联动 | 已完成 | 统一官方公开价、平台售价、管理编辑和模型广场展示 | 后端与前端全门禁、桌面/移动视觉验收通过；无 migration 或 Ent schema 变化 |
| 洛杉矶备份保留 | 已完成 | 日备份滚动保留 72 小时，升级备份保留最近 3 个目标版本且同版本只留最新一份 | 独立清理脚本和 systemd timer 已安装；首次 apply 零删除，索引、容器和公网健康正常 |
| Security Scan | 已完成 | 修复前端高危依赖和 Go 标准库漏洞基线 | `main` 与 `v1.0.3` 的 Security Scan 两个 job 均成功 |

### 进行中

- “官方 Runtime 区 + XCode Adapter”计划的 Task 3（P0 GPT/Codex）和 Task 4 Step 1（OpenAI/Codex AI 账号 OAuth、quota、cooldown、retry、failover、版本/身份与可调度状态映射）已完成；Task 4 Step 2 当前完成 GLM 代码与模拟验收，等待有效上游凭据后再做真实验收。
- 香港 `v1.0.10` 生产运行中；本轮修复了前端 pnpm 冻结安装门禁和配置默认值，已完成提交、推送、Tag、离线镜像发布及香港升级。GLM 只完成代码和模拟测试，未改变生产平台状态，Gemini 不处理。
- 模型双价格联动已整合到 `main`；基础版本 `v1.0.12` 和当前 Latest `v1.0.14` 均已部署洛杉矶并完成真实请求与计费验收，`v1.0.13` 未单独部署，香港保持 `v1.0.10`。
- `xcode-sub2api-sync` 创建计划已完成；个人 Skill 已安装，当前等待下一次正式 Sub2API Tag 盘点或本地同步请求。
- 官方 `v0.2.0` P0 F001 与 F006 已完成本地同步、逐组 rebaseline、本地提交、最终审计和完整门禁；当前等待后续阶段的单独授权，F002-F005 未获实施授权。

### 已完成

- 官方 Sub2API `v0.2.0` F001 已按固定身份 `aa236488351eb71e120fc2b6fb32e36b0374c918` 本地同步：严格 automation/delegation bootstrap、WebSocket 客户端关闭归因、client-tool/item ID、custom/function/namespace 消歧、reasoning alias/cache、非法 tool arguments、`created_at` 和 `service_tier` 已接入活动 `internal/pkg/apicompat` 与两个 OpenAI fallback service 入口。
- F001 本地门禁已通过：活动与 Official Runtime `apicompat`、带 `unit` 标签的 service 定向测试、Handler/RuntimeBridge/架构集中测试、完整 backend `go test -tags=unit ./...`、`go build ./cmd/server`、46 个同步工具测试、两套 validator 和 `git diff --check`。无 migration、Ent schema、依赖、根配置、CI、frontend、发布或部署变化；真实 Provider 验收未运行。
- `docs/upstream/sub2api-v0.2.0/revisions/001/` 保留 F001 Adapter rebaseline 前清单与 apply 证据；旧 F001 投影和 target baseline 已失效，不得复用。
- 官方 Sub2API `v0.2.0` F006 已按 A/B/C 三组本地提交：`5ec2160` 统一重试与额度冷却，`aba1dd8` 去重 SecurityAudit 配置加载日志，`3d8fb63` 保留指定模型族原生 reasoning `max`。revision 005-010 保留设计、分组、最终文档和验证状态 rebaseline 证据。
- F006 保留 XCode ProductCore/Runtime 边界：Composite/Ops/IPv6/404/singleflight/空 tool name 使用现有等价实现；无 migration、Ent schema、依赖、前端、根配置、CI、Release 或部署变化。真实 Provider acceptance 未运行。
- 个人 Skill `xcode-sub2api-sync` 已安装到 `C:\Users\xiaohuihui\.codex\skills\xcode-sub2api-sync\`，提供只读盘点和本地同步验证两种模式。无 Skill 基线会直接采用 `upstream/main` 并继续规划发布/部署；启用 Skill 后会固定正式目标 Tag/full SHA、先完成审计包与范围确认、逐功能组应用，并在 commit、push、Tag/Release 和部署前分别停止。最终压力复测、规格符合性和内容质量审查均通过，官方 `quick_validate.py` 返回 `Skill is valid!`；未写入凭据，未执行真实同步、发布或部署。
- 模型价格目录已统一解析官方公开价与平台售价：官方价保留远程目录、缓存目录、内置目录和代码回退来源；平台售价支持按平台和模型继承、显式零值、自定义覆盖及从 0 连续覆盖到无界的完整 Token 区间独立定价，实际计费继续使用平台售价乘套餐倍率和既有请求策略。
- 管理员模型价格页已改为平台模型目录优先，分别展示官方计费模式、官方价及来源和平台售价模式、售价、价差比例与状态，并提供 Token、按次、图片及结构化区间售价编辑；跨模式售价不再继承不兼容的官方字段，缺失必需字段时显示“未设置”并阻止保存。模型广场面向普通用户并列展示官方公开价（参考成本）和平台售价，不暴露管理员来源详情。
- 双价格实现已通过后端价格定向测试、`go build ./cmd/server`、完整 `make test-unit`、Go 格式检查，以及前端 209 个测试文件/1440 个测试、`typecheck`、`lint:check` 和生产构建。1440×1000 与 390×844 的本地 Edge 视觉验收确认页面无横向溢出、桌面主表无内部横向滚动且操作列完整可见；官方 `$0.04 / 图片` 与平台 `USD / 1M Token` 分离展示，跨模式显示“不可比较”，详情和编辑弹窗模式分离，清空跨模式必需售价后保存按钮禁用。`backend/migrations` 与 `backend/ent/schema` 无变化。
- 发布前独立审查已加固双价格计费边界：官方/同模式 Token 价只要求输入与输出价存在，但所有 12 个实际计费价格字段始终拒绝 NaN、无穷和负数；无官方价或跨模式 Token 售价必须四价完整且区间覆盖无缺口，跨模式按次/图片售价必须提供显式默认价。平台售价写入会在持久化前校验官方模式；Chat、Responses、Messages 会在账号选择和上游转发前按具体平台与模型预检售价；批量图片结算会保留请求平台代码。相关红绿回归、完整 `make test-unit`、前端 209 文件/1440 测试、后端与前端构建、typecheck、lint、格式和差异检查均通过，独立复审结论为 `APPROVED`。
- `v1.0.12` 已发布：Tag 指向 `f996e31849507eef709cc0388d460b7426f36c38`；主分支与 Tag 的 CI、Security Scan 及 XCode Release 全部成功。GitHub Release 已上传 `xcode_latest.tar`（42,161,195 字节，SHA-256 `b6e3ac84ed0ea4e9135dcfba544735623a013ac15de34b1c0d2c0e8a0c11affa`）和校验文件；洛杉矶已部署，香港未升级。
- `v1.0.13` 已发布：Tag 指向 `8d39fc1df0afd0fe72fb9a7665a6b5585fa25192`；模型广场不再显示“继承官方价”，平台售价与官方价恢复对齐，普通用户和管理员个人区侧边栏不再显示“可用平台”，旧 `/available-platforms` 路由仍保留。主分支与 Tag 的 CI、Security Scan、XCode Release 全部成功；对应 GitHub Release 已上传 `xcode_latest.tar`（42,163,659 字节，SHA-256 `ab826fb5f64a58f49f8190af8e65472fb323720b5db4f10b1e93c8ee71a7c8a1`）和校验文件，尚未部署生产。
- `v1.0.14` 已发布并成为 GitHub Latest Release：Tag 指向 `45bf66009284b72189cd39e635ecb432621b56e3`，同时包含模型广场/侧边栏修复和洛杉矶备份保留任务；Tag 的 CI、Security Scan、XCode Release 全部成功。Release 已上传 `xcode_latest.tar`（42,161,778 字节，SHA-256 `f8591a2766bf00fa1a533a4e3d9f09dfeb2ce50dec63adf00134ab19651ae1f7`）和校验文件，已部署洛杉矶，香港未升级。
- 洛杉矶已从 `v1.0.12` 升级到 `v1.0.14`：运行镜像为 `sha256:d5e355d777ec8315f5b2e6abe5793cf98e5806b1bc780c90c008f06e1b3b8251`，revision 为 `45bf66009284b72189cd39e635ecb432621b56e3`；旧镜像保留为 `xcode:rollback-v1.0.12-before-v1.0.14`。升级前完整备份位于 `/opt/xcode/backups/v1.0.12-before-v1.0.14-20260825T074321Z`，Release 与备份 SHA-256 均复核通过。
- 洛杉矶 `v1.0.14` 验收通过：应用、PostgreSQL、Redis 均 healthy，7 用户、9 个活动 API Key、2 平台、15 账号、20 订阅和 253 条 migration 保持不变；模型广场返回 2 个平台、7 个模型且 7 个模型均同时包含官方价和平台售价，管理员价格目录的平台 7 返回 5 条带官方来源和售价的记录。问题用户的临时 Key 完成 Responses 与 Chat 请求并生成 usage `99815-99816`，费用分别为 `0.004825 USD` 和 `0.000205 USD`，均归属平台 7、账号 15、订阅 18 并从订阅扣费；临时 Key 已软删除且查询返回 404，usage 审计记录保留。

- 官方 `v0.1.179` 的 42 个纯 `apicompat` 协议源码、测试和 fixture 已复制到 `backend/internal/runtime/sub2api/upstream/apicompat/`；该目录独立 `go test` 通过，但当前生产调用仍使用 XCode 现有 `internal/pkg/apicompat`，因此没有改变线上行为。
- P0 协议回归已补齐：OAuth、API Key 的 Responses/Chat/Messages 非流式缓冲读取失败均返回可切换账号的 `UpstreamFailoverError`，且客户端未写出部分响应；Responses→Chat fallback 已接入账号/API Key 隔离的短期 reasoning 缓存，可恢复 encrypted-only reasoning，流式 reasoning item 也会在上游完整终态后写入同一隔离缓存。
- WebSocket later-turn 429 已按官方 `v0.1.179` 语义同步：保留当前 turn，移除不可跨账号使用的 `previous_response_id`，补齐历史输入和工具上下文，更换账号后只重放当前 turn。HTTP bridge 会跨 turn 继承 client tool mapping/lowered declarations，恢复 custom、namespace、tool_search 声明并将历史降级为 function 协议；已修复适配后 body 被 Go 变量遮蔽而未用于上游请求的问题。
- Runtime terminal ownership 已复核：OpenAI/Messages executor 的 terminal sink 对重复记录返回 `ErrTerminalAlreadyRecorded`，已有终态时外围不再补写；WebSocket 每个成功 turn 只提交一次 usage，失败且无可计费结果时不提交成功用量。相关 `apicompat`、service、handler 定向测试、包编译和 `go build ./cmd/server` 通过。

- Responses input-token preflight 已完成 Runtime 接入：`/v1/responses/input_tokens` 及 bare/Codex alias 归一化、非计费 executor、官方 OpenAI 上游透传、Relay/Grok/404 本地估算和 Ops token-count 分类均已实现；专项 service、handler、applicationgateway、architecture 测试与 `go build ./cmd/server` 通过。

- 已修复 Responses WebSocket GET 握手没有请求体时缺少平台资产解析的问题：首个 `response.create` 读取模型后由 ProductCore authorizer 注入业务平台、调度命名空间和订阅；回归测试锁定平台 1、调度命名空间 -2、订阅 20。
- 香港隔离副本已用 `gpt-5.6-sol` 完成 Chat、Responses、SSE、普通 HTTP 403 跨账号切换、失败不扣费、WebSocket 正常两轮和 later-turn 429 当前轮重放。最终 429 场景从账号 20 切到账号 15，替换上游收到完整 3 项历史/工具上下文且无 `previous_response_id`；两条成功 usage 合计 `0.000330 USD`，套餐增量完全一致，余额不变。
- 最终门禁通过：Handler/Routes 定向测试、带 `unit` tag 的完整 service 测试、Official Runtime 独立包、applicationgateway、architecture、RuntimeBridge、46 个同步工具测试、同步清单校验、`go build ./cmd/server`、`gofmt -d` 和 `git diff --check`；`backend/migrations` 与 Ent schema 无变化。
- 香港隔离容器、Redis、测试数据库 `xcode_runtime_test`、测试镜像和远端临时二进制已删除；生产容器健康，生产计数保持 migration 253、7 用户、25 API Key、2 平台、20 AI 账号、21 订阅、40,903 usage，`https://api.ai-work.vip/health` 正常。

- GLM 额度适配已接入现有 Runtime/ProductCore 边界：标准 `open.bigmodel.cn`/`api.z.ai` Coding Plan quota endpoint、5h/weekly 解析、Header override、401/403/429/5xx 分类和 `accounts.extra` 事实写回；后台只读接口为 `GET /api/v1/admin/accounts/:id/glm-quota`，响应不含凭据。GLM 账号仍以 `AccountPlatform=openai` 走现有 Chat/Responses、账号测试、冷却和失败切换，按 `platform_id` 独立选池。新增后端服务/Handler/平台池测试通过，数据库 migration 与 Ent schema 无变化。

- `xiaohuihuitop/xcode` 已建立 `main` 分支，当前代码基线来自已验证的 `my2.0`。
- 产品发布契约已确定为 `xcode:latest` 与 `xcode_latest.tar`，不发布 GHCR。
- ProductCore / GatewayRuntime / Sub2API Adapter 边界和 OpenAI 当前范围已写入正式文档。
- 远程 `main` 已重建为单根提交，新的注释 Tag `v1.0.0` 指向该根提交；旧 `v1.0.0`、`v1.0.1` 已删除。
- `v1.0.0` 的 XCode Release、后端单元测试、PostgreSQL 集成测试、前端门禁和 lint 全部成功，已发布 `xcode_latest.tar` 与 SHA-256 文件。
- RuntimeBridge v1 契约、本地端口、Sub2API Driver、OpenAI 端口适配和架构守卫已回迁到当前工作区；提交为 `c5272a4`。
- 后端 `make test-unit`、`go build ./cmd/server` 和 `git diff --check` 通过；Tag CI 与 XCode Release 均成功。
- `v1.0.1` 已发布 `xcode_latest.tar`（42,019,683 字节）及 SHA-256 文件。
- 已从新加坡测试环境迁移到香港服务器：PostgreSQL 逻辑备份、Redis 快照、`data/`、Compose 与环境配置均完成哈希校验后恢复；迁移前备份保存在两端的 `/opt/sub2api/backups/xcode-v1.0.1-hk-20260821T0945`。
- 源/目标数据对账一致：7 用户、16 API Key、2 平台、18 账号、21 订阅、43,197 条历史使用记录和 614 个 Redis 键。
- 香港服务器的 `xcode:latest`、PostgreSQL 和 Redis 均健康；Chat Completions 与 Responses 均返回 200，并新增两条订阅计费用量记录。
- 香港服务器已配置 Nginx 站点 `api.ai-work.vip`：应用继续仅监听 `127.0.0.1:8080`，公网 80 端口跳转 HTTPS，443 端口反代到应用；Let's Encrypt 证书有效期至 2026-11-19 且已启用自动续期。
- 域名验收通过：`https://api.ai-work.vip/health` 返回 `{"status":"ok"}`，根路径返回 200，未带 API Key 的 Chat Completions 与 Responses 请求均返回应用预期的 401。
- 使用活动平台的真实上游模型 `gpt-5.3-codex-spark` 通过域名完成 Chat Completions、Responses 和 SSE，各返回 200；三条成功用量记录写入，合计 `total_cost=0.0041622`，扣费来源均为 `subscription`，订阅实例 20 的窗口用量增加，用户余额保持不变。
- 首次用不在上游目录中的 `gpt-4o-mini` 得到 502 且未产生用量或扣费；平台规则为 `gpt-*`，上游账号 15 的 `/models` 返回 7 个真实模型。应用 `/v1/models` 当前返回空列表，原因是数据库只配置了通配模型规则而非精确模型名。
- 管理员登录与后台只读接口验收通过：仪表盘、用户、平台、账号、订阅、用量、支付仪表盘/配置/套餐/服务商/订单、账号可用性和入口拒绝健康状态均返回 200；可撤销的平台、套餐和 API Key CRUD 已验证并清理临时数据。
- `gpt-5.6-sol` 已在香港域名上完成 Chat Completions、Responses 和 Chat SSE 真实调用；三种请求均返回 200。最新 SSE 写入平台 1、订阅实例 20、`billing_source_type=subscription` 的 usage 记录，套餐窗口用量相应增加，临时 API Key 已删除并验证返回 404。
- 曾发生一次 `gpt-5.6-sol` SSE 在约 60 秒后返回 502；应用日志将其记录为上游瞬态状态，未写 usage 或扣费。随后同一路径 SSE 在约 3.8 秒成功完成，当前不能归因为 XCode 缺陷。
- `glm` 平台（ID 2）处于 disabled；当前候选 OpenAI 账号的上游模型目录仅含 GPT 系列，不含 `glm-5.2`，因此没有执行 GLM 真实调用或擅自启用该平台。
- `glm-5.2` 真实请求已确认路由到平台 2、账号 17，但上游返回 `401 unauthorized`；应用记录 `account_disabled_auth_error` 并返回 502，未写 usage 或扣费。根因是 GLM 上游账号凭据无效/失效。
- Security Scan 根因已确认：前端官方审计新增 `nanoid` 高危通报，锁定版本为 `3.3.17`；后端 `govulncheck` 命中 `GO-2026-6090`（crypto/tls）和 `GO-2026-6218`（net/url），均在 Go `1.26.6` 修复。已将 nanoid 升级到 `3.3.18`，并将 Go/CI 基线升级到 `1.26.6`。
- 前端安全修复后的官方审计仅剩现有 `xlsx` 两项豁免，异常校验通过；`pnpm@9` 冻结安装、201 个测试文件/1357 个测试和生产构建均通过。受网络限制，本机未能安装 `govulncheck` 或 Go `1.26.6` 工具链。
- 已将所有 CI/Release 工作流的 Go 版本门禁统一到 `1.26.6`；`v1.0.3` 的 CI、Security Scan 和 XCode Release 全部成功，`xcode_latest.tar` 已由 Release 流程生成。
- 香港服务器已升级到 `v1.0.3`：远程镜像 SHA-256 与 Release 一致，应用镜像为 `sha256:8725a35b3cc2ab168f247c92dbc1b8ac0a7800177e1aef3c3954f8c822f5feb9`；PostgreSQL、Redis 和应用容器均健康，旧镜像保留为 `xcode:rollback-before-v1.0.3`。
- 升级前备份保存在 `/opt/sub2api/backups/xcode-v1.0.3-hk-20260821T103702Z`，包含 Compose、环境文件、应用 `data/` 压缩包和 PostgreSQL 逻辑备份。
- 已确认继续使用 Sub2API 作为唯一生产 Runtime，CAP 替换计划暂缓；`v1.0.4` Tag 和 XCode Release 已发布，CI、Security Scan、后端、前端、lint、PostgreSQL 集成测试和离线镜像校验均成功。
- 香港服务器已升级到 `v1.0.4`：运行镜像为 `sha256:e5e150200f430ef2d9302baa583577a0b85f5ca174c77d8409af6becd2544eb4`，revision 为 `c0cbd4a805b8b25cb206a068579d4eec9b415bec`；应用、PostgreSQL 和 Redis 均健康，旧镜像保留为 `xcode:rollback-before-v1.0.4`。
- `v1.0.4` 升级前完整备份位于 `/opt/sub2api/backups/xcode-v1.0.4-hk-20260822T015519Z`，Compose、环境文件、`data/`、PostgreSQL 逻辑备份、Redis 快照和计数清单均通过 SHA-256 复核；升级前后 7 用户、19 API Key 物理行、2 平台、18 账号、21 订阅和 42,231 条 usage 完全一致，没有 schema 或业务数据迁移。
- `v1.0.4` 通过域名完成 `gpt-5.6-sol` Chat、Responses 和 Chat SSE 真实调用，三次均返回 200；生成 usage `62976-62978`，均归属平台 1、账号 15、订阅 20、`billing_source_type=subscription`，每次费用 `0.00482`，套餐用量合计增加 `0.01446`。两个临时 Key 已软删除且活动记录为 0。
- 真实验收首次 Chat 曾收到账号 15 上游 502，未产生 usage 或扣费；随后 Chat、Responses、SSE 连续成功。`v1.0.3..v1.0.4` 仅有文档变化，因此该次失败判定为上游瞬态波动，不是新镜像回归。
- `v1.0.4` 管理面只读验收通过：平台、支付套餐、账号列表、账号可用性和仪表盘接口均返回 200；公网 `/health` 返回 200，未认证 Chat/Responses 返回 401。
- Codex 平台账号 15、19、20 均为 active、可调度且无冷却/错误状态；账号 19、20 的 `gpt-5.6-sol` 单账号 SSE 连接测试均返回 200 和成功终态。
- 使用临时 Key 23 通过香港域名完成 18 次真实网关请求，全部返回 200 并生成 usage `62985-63002`；所有记录均归属平台 1、订阅实例 20、`billing_source_type=subscription`，`total_cost=actual_cost`，合计套餐扣费 `0.040837`。
- 普通 HTTP 客户端请求会先选中账号 19，但该账号上游返回 403；Sub2API 在同账号重试后自动切换到账号 15/20 并成功完成请求，真实验证了账号间 403 失败切换。携带官方 Codex 身份头的 3 次 Responses 请求均由账号 19 成功完成并写入 usage `63000-63002`，说明账号 19 是 Codex 客户端限定上游而非无效账号。
- 临时 Key 23 已软删除，API 查询返回 404；数据库中活动行 0、审计删除行 1，18 条 usage 按设计保留用于计费审计。
- API Key 新增 `allow_all_subscriptions` 开关：新建 Key 默认授权当前活动平台、全部套餐和余额；历史有套餐关联的 Key 迁移为动态全部套餐，纯余额 Key 不扩大权限；新购买套餐无需改 Key 即可参与计费。
- 所有邮箱/OAuth 注册路径会 fail-open 创建幂等默认 Key，首次成功登录会补建缺失 Key；管理 Key 摘要和用户 Key 表单已切换到“全部套餐/余额”语义。
- 本地验证通过：相关 Vitest 19 个文件/105 个测试、Vue typecheck、生产构建、Go service 全量单测、handler 编译、repository/migrations/ent 测试和 `go build ./cmd/server`；整组 handler 运行态测试因外部依赖长时间无输出已停止，未将其计为通过。
- 充值界面调整已完成本地验证：隐藏快捷充值按钮，默认金额为 `1`，从支付设置动态读取到账倍率并显示充值金额/到账额度结果，手续费仍独立计入实付金额；充值相关 12 个测试文件/97 个测试、Vue typecheck 和生产构建通过。
- `v1.0.8` 已发布：`main`、注释 Tag 的 peeled commit 和镜像 revision 均为 `a01d2272d0e549d0ec2c18ab914b619b67c96503`；GitHub Release 已上传 `xcode_latest.tar`（42,044,847 字节）及 SHA-256 文件。
- 香港服务器已部署 `v1.0.8`，运行镜像为 `sha256:3edb6c8e15000f402cfb95ba83a73d268310607699d494b822a7181877c1fe65`，容器健康；旧镜像保留为 `xcode:rollback-v1.0.7`。
- `v1.0.8` 升级前完整备份位于 `/opt/sub2api/backups/xcode-v1.0.7-before-v1.0.8-hk-20260823T160719Z`。升级前后 7 用户、25 API Key、2 平台、20 账号、3 套餐、21 用户订阅保持一致，schema migration 仍为 253；3 次 GPT 成功测试使 usage 从 40,918 增至 40,921。
- `v1.0.10` 已发布：commit `e61b0cf`，Tag `v1.0.10`，GitHub Release 已生成并校验 `xcode_latest.tar`（42,108,911 字节）及 SHA-256 `537af964803620d686fbb1974b4036e70bde440d5d5ee1d484dee6ac9cc4a431`；CI、Security Scan、Release 全部成功。
- 香港已升级到 `v1.0.10`，镜像为 `sha256:6c354460a9b1e3fd94327e816722f771c6c71437b31f24a691195a356e150f73`，旧镜像保留为 `xcode:rollback-v1.0.8`。升级前完整备份位于 `/opt/sub2api/backups/xcode-v1.0.8-before-v1.0.10-hk-20260824T094719Z`；升级后 7 用户、25 API Key、2 平台、20 账号、21 订阅、40,865 usage，schema migration 253，公网健康、未授权 401、管理员登录和平台接口均验收通过。
- 香港生产终检通过：HTTPS 健康检查、未认证 401、管理员登录、平台、套餐、模型价格和模型广场接口正常；平台与模型广场均为 `codex`、`glm`，模型价格已生效。`gpt-5.6-sol` 的 Responses、Chat Completions 和 SSE 均成功并按订阅扣费，失败请求不扣费。
- usage `63007-63009` 在数据库中均关联订阅 20 和套餐“大包”；接口中 `subscription_plan_id`、`plan_name_snapshot` 为空是响应映射未展开，不影响数据库归属与扣费。
- 洛杉矶跨日订阅额度缺陷已修复并部署为 `v1.0.11`：计费候选不再复用展示归一化列表，保留持久化窗口信号后由现有原子维护链重置数据库和缓存。双订阅回归、完整后端单测和构建通过；问题 Key 的 Responses 真实请求由 403 恢复为 200，订阅 18 的日窗口从 2026-08-22 推进到 2026-08-24 并新增一笔 `0.004835 USD` 用量。管理员登录/平台接口为 200，未授权入口为 401，所有容器健康。
- 洛杉矶 `v1.0.11` 运行镜像为 `sha256:6b6b38933007a1c5d31893d94f5751a98368bf617a8271ddcafd123515e40d9c`，`v1.0.10` 回滚标签为 `xcode:rollback-v1.0.10-before-v1.0.11`。升级前备份位于 `/opt/xcode/backups/v1.0.10-before-v1.0.11-20260824T153302Z`，离线镜像及校验文件位于 `/opt/xcode/releases/v1.0.11/`。
- 洛杉矶已从 `v1.0.11` 升级到 `v1.0.12`：运行镜像为 `sha256:f7e944694105c68c56184f63a7de78a17db194cf449132fb14d285ae80fa4ae2`，revision 为 `f996e31849507eef709cc0388d460b7426f36c38`；旧镜像保留为 `xcode:rollback-v1.0.11-before-v1.0.12`。升级前备份位于 `/opt/xcode/backups/v1.0.11-before-v1.0.12-20260825T054519Z`，离线包位于 `/opt/xcode/releases/v1.0.12/` 且 SHA-256 与 Release 一致。
- 洛杉矶 `v1.0.12` 验收通过：应用、PostgreSQL、Redis 健康，7 用户、16 个 API Key 物理行、9 个活动 Key、2 平台、15 账号、20 订阅和 253 条 migration 保持不变。管理员价格目录与普通用户模型广场均返回 7 个模型且同时包含官方价和平台售价；问题账号的活动 Key 完成 Responses 与 Chat 请求并生成 usage `99808-99809`，费用分别为 `0.004825 USD` 和 `0.000205 USD`，均归属订阅 18、平台 7、账号 15。用户此前提供的 Key 已不在当前数据库中，因此该 Key 返回 401，不是额度或版本错误。
- 洛杉矶备份保留已于 2026-08-25 部署并验收：仓库脚本、service、timer 与服务器安装文件 SHA-256 一致，`systemd-analyze verify` 通过；首次 service 为 `Result=success`、`planned=0`，未删除当前 7 份日备份（232 MB）和 2 份升级备份。timer 为 `enabled/active`，下次触发为 `2026-08-26 04:31:01 UTC`；`backup-index.txt` 保持既有升序格式，磁盘剩余约 4.0 GB，应用、PostgreSQL、Redis 均 healthy，`https://www.aitodo.work/health` 返回 200。

### 下一步

1. GitHub Latest Release 为 `v1.0.14`，洛杉矶运行 `v1.0.14`，香港继续运行 `v1.0.10`；香港升级仍需单独确认。
2. 继续 Task 4 Step 2：等待或配置有效 GLM 上游凭据后，执行真实 Chat/Responses、账号测试和额度查询验收；401/上游错误不得归因于 XCode 通过。
3. 在 GLM 真实验收条件满足后，再按相同边界同步 DeepSeek；Gemini 不处理。
4. 洛杉矶跨日订阅修复已纳入正式 Git 提交；当前生产镜像已部署并可回滚，后续发版继续保留该修复。
5. 洛杉矶备份保留 timer 已启用；后续确认首次出现超过 72 小时的日备份后，检查 journal 中的逐项删除原因和剩余索引即可，无需人工按数量清理。
6. 下一次检查或同步官方 Sub2API 正式 Tag 时调用 `$xcode-sub2api-sync`；默认只做到本地验证，提交、推送、Tag/Release 和服务器部署仍需分别确认。
7. F001 与 F006 已完成本地提交；下一阶段如需 push、Tag/Release、离线包或生产部署，仍需分别确认并在对应阶段重新验证。

### 阻塞与待确认

- GitHub 贡献者统计刷新可能有延迟。
- `glm-5.2` 当前路由和平台配置正常，但真实请求仍因上游故障返回 502；不阻塞 GPT 主线。
- `/v1/models` 对仅含通配模型规则的平台返回空目录；请求路由不受影响，但客户端无法自动发现实际可用模型。调整为精确模型规则会改变线上平台配置，需用户确认。
- 上线前仍需处理订单套餐快照、退款幂等等风险，详见 `docs/ROADMAP.md`。
- 账号 19 的上游要求官方 Codex 客户端身份：普通 HTTP 客户端会收到 403 并自动切换到其他账号，官方 Codex 身份请求可正常使用该账号；这是上游兼容性边界，不影响 Codex 主线。
- 本机临时文件 `C:\Users\xiaohuihui\AppData\Local\Temp\xcode-runtime-v01179-test-fixed`（143,337,762 字节）仍存在；当前删除策略拒绝了清理命令，不影响仓库、服务器或发布结果。
- 根 `Dockerfile` 默认 `GOLANG_IMAGE=golang:1.26.5-alpine`，但 `backend/go.mod` 已要求 Go 1.26.6；本轮镜像使用显式 `GOLANG_IMAGE=golang:1.26.6-alpine`/预编译发布路径完成，后续修改根配置需单独确认。
- F001 与 F006 真实 Provider acceptance 未运行；本地/mock 测试不能证明生产账号路由、上游响应、计费和 usage 已验收。

### 关联决策

- `docs/memory/决策/2026-08-20-XCode独立仓库与v1发布线.md`
- `docs/memory/决策/2026-08-20-XCode独立仓库迁移交接.md`
- `docs/memory/决策/2026-08-21-生产验收默认模型.md`
- `docs/memory/决策/2026-08-22-Sub2API作为唯一Runtime.md`
- `docs/memory/决策/2026-08-24-计费候选保留原始订阅窗口.md`
